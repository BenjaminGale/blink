cabal-version: 3.0
name: blink
version: 0.1.0.0
license: Apache-2.0
license-file: LICENSE
author: Benjamin Gale
maintainer: benjaminiangale@gmail.com
build-type: Simple
synopsis: An immediate-mode UI library for Haskell desktop applications
description:
    Blink is an immediate-mode UI library for Haskell desktop applications:
    the whole UI tree is rebuilt from scratch every frame as a pure function
    of application state, rather than retained and mutated incrementally.
    State changes only through modifiers dispatched by the UI. Blink does
    not own the main loop -- a backend (SDL2, in the included demo) drives
    the loop and calls into Blink once per frame.

    Start with "Blink" for the module guide, "Blink.App" for how to define
    an application and wire it up to a backend, and "Blink.UI" for the
    monad views are built from.

common defaults
    ghc-options: -Wall
    default-language: Haskell2010
    build-depends:
        base ^>=4.18.3.0,
        containers ^>=0.6.7,
        text ^>=2.0.2

library
    import: defaults
    exposed-modules:
        Blink
        Blink.App
        Blink.Attribute
        Blink.Controls
        Blink.Controls.Button
        Blink.Controls.Checkbox
        Blink.Controls.Control
        Blink.Controls.Element
        Blink.Controls.Label
        Blink.Controls.ProgressBar
        Blink.Controls.RadioButton
        Blink.Controls.TextInput
        Blink.Geometry
        Blink.Input
        Blink.Layout
        Blink.Layout.Border
        Blink.Layout.Box
        Blink.Layout.Constraints
        Blink.Rendering
        Blink.Style
        Blink.Style.Defaults
        Blink.UI
        Blink.UI.Element
        Blink.Update
    hs-source-dirs: src

executable demo
    import: defaults
    ghc-options: -threaded
    main-is: Main.hs
    other-modules:
        UI,
        Theme,
        Rendering
    build-depends:
        blink,
        sdl2 ^>=2.5.5,
        sdl2-ttf ^>=2.1.3
    hs-source-dirs: app

test-suite blink-tests
    import: defaults
    type: exitcode-stdio-1.0
    hs-source-dirs: test
    main-is: Main.hs
    other-modules:
        Blink.AppSpec
        Blink.Controls.ButtonBehaviour
        Blink.Controls.ButtonSpec
        Blink.Controls.CheckboxSpec
        Blink.Controls.ControlBehaviour
        Blink.Controls.ControlSpec
        Blink.Controls.ElementBehaviour
        Blink.Controls.ElementSpec
        Blink.Controls.FixedFocusBehaviour
        Blink.Controls.LabelSpec
        Blink.Controls.ProgressBarSpec
        Blink.Controls.RadioButtonSpec
        Blink.Controls.TextInputSpec
        Blink.Controls.ToggleBehaviour
        Blink.Generators
        Blink.GeometrySpec
        Blink.InputSpec
        Blink.Interaction
        Blink.InteractionSpec
        Blink.LayoutSpec
        Blink.Style.DefaultsSpec
        Blink.UISpec
        Blink.UpdateSpec
    build-depends:
        blink,
        hspec ^>=2.11.17,
        QuickCheck ^>=2.18.0.0
